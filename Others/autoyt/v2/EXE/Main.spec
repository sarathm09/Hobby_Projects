# -*- mode: python -*-
a = Analysis(['../Main.py'],
             pathex=['E:\\My Documents\\GitHub\\Hobby_Projects\\Others\\autoyt\\v2\\EXE'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='Main.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='Main')
